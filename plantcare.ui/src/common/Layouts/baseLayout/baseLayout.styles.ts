const container = {
  width: '100%',
  height: '100%'
};

const wrapper = {
  width: '100%',
  height: '90%',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center'
};

export default {
  container,
  wrapper
};
