const authContainer = {
  width: '100%'
};

const authNavbar = {
  backgroundColor: '#101316',
  width: '100%',
  height: '60px',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'flex-end'
};

const authBtn = {
  mr: '15px'
};

export default {
  authContainer,
  authNavbar,
  authBtn
};
